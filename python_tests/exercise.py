#Create a Python function that takes an integer ( n ) as input and generates a dictionary containing pairs
#( (i, i^2) ) for all integers ( i ) from 1 to ( n ) (inclusive). The function should then return this dictionary.

def foda:
    n = int(input('Digite um número inteiro: '))
    for i in range (n):
